services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    volumes:
      # Persist data and results folders
      - ./data:/app/data
      - ./results:/app/results
    environment:
      - NODE_ENV=development
      - PYTHONUNBUFFERED=1
